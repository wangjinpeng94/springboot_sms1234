spring:
  cloud:
    config:
      uri: http://127.0.0.1:12000 #连接的SpringCloudConfig服务端地址
      name: movie #配置文件的前缀
      profile: dev #配置文件的后缀
      label: master #需要获取的仓库分支名称
    rabbitmq:
      host: 192.168.2.11